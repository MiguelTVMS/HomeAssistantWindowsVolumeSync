#!/usr/bin/env bash
# .githooks/pre-commit
# Runs before every commit. Blocks the commit if AI instruction symlinks are broken.
#
# Install once per clone:
#   git config core.hooksPath .githooks

REPO_ROOT="$(git rev-parse --show-toplevel)"
exec bash "$REPO_ROOT/scripts/validate-symlinks.sh"
